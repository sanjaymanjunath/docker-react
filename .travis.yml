sudo: required
services:
    - docker

before_install:
    - docker build -t manjunathkontham/docker-react -f Dockerfile.dev .

script:
    - docker run manjunathkontham/docker-react npm run test -- --coverage
